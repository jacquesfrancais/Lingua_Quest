Run lingua_quest_db_schema.sql with the following in the terminal:

mysql -u username -p < lingua_quest_db_schema.sql

// username - the user name that you choose.
BTW I will send you the code for db_config.php in an email

____________________________________________________________________
After running the script, you can verify:

USE lingua_quest_db;
SHOW TABLES;

_____________________________________________________________________

The TSV (.tsv) files can be used to load the data into database tables.